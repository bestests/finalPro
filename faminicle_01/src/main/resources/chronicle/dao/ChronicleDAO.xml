<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="chronicle.dao.ChronicleDAO">
	<resultMap type="chronicle" id="chronicleMap">
		<result column="no" property="no" />
		<result column="content" property="content" />
		<result column="reg_date" property="regDate" />
		<result column="pic_date" property="picDate" />
		<result column="file_path" property="filePath" />
		<result column="row_num" property="rNum" />
	</resultMap>
	<select id="selectList" resultMap="chronicleMap">
		select *
		  from listtest
		 order by reg_date desc
		 limit 24
	</select>
	<select id="selectPrevList" parameterType="page" resultMap="chronicleMap">
		select t1.*
		  from (
		  		select t.* 
		  		  from (
				  		select *
				  		  from listtest
				  		 where reg_date >= #{startDate}
				  		 order by reg_date asc
		  		  		)as t
		  		  where no > #{pageNo} 	
		  		  limit 24	
		  		)as t1
		 order by reg_date desc  		
	</select>
	
	<select id="selectNextList" parameterType="page" resultMap="chronicleMap">
		select *
		  from listtest
		 where reg_date <![CDATA[<=]]> #{endDate}
		   and no <![CDATA[<]]> #{pageNo}
		 order by reg_date desc
		 limit 24 
	</select>
	
	<insert id="insertMember" parameterType="members">
		insert into fam_member(id,pass,e_mail,name,birthday,tel)
		values(#{id},#{pass},#{eMail},#{name},#{birthday},${tel})
	</insert>
	
	<select id="checkId" parameterType="members" resultType="int">
		select count(*)
		from fam_member
		where id = #{id}
	</select>
	
	
	<!-- 회원 관련 쿼리  -->
	<resultMap type="members" id="loginMap">
		<result column="mem_no" property="memNo" />
		<result column="id" property="id" />
		<result column="pass" property="pass" />
		<result column="e_mail" property="eMail" />
		<result column="pic_File_Path" property="picFilePath" />
		<result column="name" property="name" />
		<result column="birthday" property="birthday" />
		<result column="tel" property="tel" />
	</resultMap>
	<select id="loginCheck" parameterType="LoginCheck" resultMap="loginMap">
		select *
		from fam_member
		where id= #{id} and pass=#{pass}
	</select>
	
	<insert id="insertpic" parameterType="regist">
		
			insert into fam_pic(MEM_NO, TITLE, PIC_DATE, PIC_FILE_PATH, LAT, LNG)
			values(#{memNo}, #{title}, #{picDate}, #{picFilePath}, #{lat}, #{lng})
		
	</insert>
	
	<select id="checkPass" parameterType="members" resultType="members">
		select pass
		  from fam_member
		 where id = #{id} 
	</select>
	
	<select id="memberInfo" parameterType="members" resultMap="loginMap">
		select * 
		from fam_member
		where id = #{id}
	</select>
	
	<update id="updateMember" parameterType="members">
		update fam_member
		set pass   = #{pass}, 
			e_Mail = #{eMail},
			tel    = #{tel}
		where id = #{id} 
	</update>
	
	<update id="updataMemberPic" parameterType="members">
		update fam_member
		set pic_File_Path = #{picFilePath}
		where id = #{id}
	</update>

	
	
	<!-- 기념일 설정 쿼리  -->
	<resultMap type="eventday" id="eventdayMap">
		<result column="ev_no" property="evNo" />
		<result column="mem_no" property="memNo" />
		<result column="ev_title" property="evTitle" />
		<result column="ev_start" property="evStart" />
		<result column="ev_end" property="evEnd" />
	</resultMap>
	<!-- member 번호로 기념일 조회 -->
	<select id="selectEventByMem" parameterType="int" resultMap="eventdayMap">
		select *
		  from eventday
		 where mem_no = #{memNo} 
	</select>
	<!-- 기념일 등록  -->
	<insert id="insertEvent" parameterType="eventDay" useGeneratedKeys="true" keyProperty="evNo">
		insert into eventday(mem_no, ev_title, ev_start, ev_end)
		values(#{memNo}, #{evTitle}, #{evStart}, #{evEnd})
	</insert>
	<!-- insert 후 1개 기념일 조회  -->
	<select id="selectEventOne" parameterType="int" resultMap="eventdayMap">
		select *
		  from eventday
		 where ev_no = #{evNo} 
	</select>
	<!-- 기념일 삭제   -->
	<delete id="deleteEvent" parameterType="int">
		delete
		  from eventday
		 where ev_no = #{evNo} 
	</delete>
	
	
</mapper>